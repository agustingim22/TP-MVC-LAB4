@model IEnumerable<TPFINALLAB4MVC.Models.PartidoDetalle>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.golesJugador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.golesRival)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cantRojas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cantAmarillas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPartido)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.golesJugador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.golesRival)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cantRojas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cantAmarillas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPartido)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
